int doUnion(int a[], int n, int b[], int m)  {
        set<int>result;
        for(int i=0;i<n;i++)
        {
            result.insert(a[i]);
        }
        for(int i=0;i<m;i++)
        {
            result.insert(b[i]);
        }
        return result.size();
    }
